# BG96 省電力モード解説：PSM と eDRX

- **PSM**：Power Saving Mode（省電力モード）
- **eDRX**：Extended Discontinuous Reception（拡張間欠受信）

## 概要

IoT デバイスの電池寿命を延ばすために、LTE-M（Cat-M1）には2種類の省電力モードが用意されている。

| | PSM | eDRX |
|---|---|---|
| 省電力効果 | ★★★★★（最大） | ★★★☆☆（中程度） |
| レイテンシ | 高い（分〜時間単位） | 低い（秒〜分単位） |
| 着信受信 | TAU 復帰時のみ | eDRX サイクルごとに可能 |
| 主な用途 | 定期センシング送信 | リアルタイム性が少し必要な用途 |

---

## PSM（Power Saving Mode）

### しくみ

```
[アクティブ] ──active timer経過──> [PSM スリープ] ──TAU経過──> [アクティブ(再登録)] ──> [PSM スリープ] ...
     │                                    │                          │
  AT通信可                            UART停止                   LTE再登録
  LTE接続中                           電力極小                   通信後またスリープ
```

- **Active Timer（T3324）**：最後のネットワーク通信から PSM に入るまでの時間
- **TAU Timer（T3412）**：PSM から自律復帰するまでの周期（Tracking Area Update）
- スリープ中は UART も完全停止。AT コマンドへの応答なし

### BG96 AT コマンド

```
# PSM 有効化（TAU=1分、ActiveTime=10秒）
AT+CPSMS=1,,,"10100001","00000101"

# 設定確認
AT+CPSMS?

# PSM 無効化
AT+CPSMS=0
```

#### タイマー値のエンコード（TS 24.008 形式）

**T3412（TAU）8ビット文字列：**

| ビット 7-5 | 単位 | ビット 4-0 | 例 |
|---|---|---|---|
| `000` | 10 分 | 5ビット値 | `00000011` = 30 分 |
| `001` | 1 時間 | 〃 | `00000001` = 1 時間 |
| `010` | 10 時間 | 〃 | `00000001` = 10 時間 |
| `011` | 2 秒 | 〃 | `00011110` = 60 秒 |
| `100` | 30 秒 | 〃 | `00000010` = 60 秒 |
| `101` | 1 分 | 〃 | `10100001` = **1 分** ← テスト値 |
| `110` | 320 時間 | 〃 | — |
| `111` | 無効 | — | — |

**T3324（Active Time）8ビット文字列：**

| ビット 7-5 | 単位 | ビット 4-0 | 例 |
|---|---|---|---|
| `000` | 2 秒 | 5ビット値 | `00000101` = **10 秒** ← テスト値 |
| `001` | 1 分 | 〃 | `00000001` = 1 分 |
| `010` | 6 分 | 〃 | `00000001` = 6 分 |
| `111` | 無効 | — | — |

### DTR による強制起動

PSM 中でも `MODULE_DTR_PIN` を LOW にパルス入力することで強制復帰できる。

```cpp
// DTR パルス（50ms LOW → HIGH）で PSM から強制復帰
digitalWrite(MODULE_DTR_PIN, LOW);
delay(50);
digitalWrite(MODULE_DTR_PIN, HIGH);
```

> **注意：** ネットワークが要求値と異なる TAU を割り当てる場合がある。
> 実際の TAU は `AT+CEREG=4` + `AT+CEREG?` の拡張応答で確認できる。

---

## eDRX（Extended Discontinuous Reception）

### しくみ

```
[アクティブ] ─────────────────────────────────────────────>
                  ↑起動    ↑起動    ↑起動
[eDRX サイクル]  [スリープ]──[ページング確認]──[スリープ]──[ページング確認]──...
                  ←──── eDRX cycle ────→
```

- **eDRX サイクル**：スリープとページング確認を繰り返す周期
- **ページング確認ウィンドウ（PTW）**：着信（SMS・ダウンリンク）を受け取れる時間帯
- PSM と異なり LTE 接続は維持したまま。再登録のオーバーヘッドなし
- AT コマンドはサイクルの隙間に数秒の遅延が生じることがある

### BG96 AT コマンド

```
# eDRX 有効化（LTE-M、サイクル約 20.48 秒）
AT+CEDRXS=2,4,"0010"
  # mode=2: 有効化 + URC 通知
  # AcT=4 : LTE-M (Cat-M1)
  # "0010": eDRX 値（下表参照）

# ネゴシエーション結果の確認
AT+CEDRXRDP

# eDRX 無効化
AT+CEDRXS=0
```

#### eDRX 値と周期（LTE-M）

| 値 | サイクル |
|---|---|
| `"0000"` | 5.12 秒 |
| `"0001"` | 10.24 秒 |
| `"0010"` | 20.48 秒 ← テスト値 |
| `"0011"` | 40.96 秒 |
| `"0100"` | 61.44 秒 |
| `"0101"` | 81.92 秒 |

> **注意：** ネットワーク側が希望値を認可しない場合がある。
> 実際の値は `AT+CEDRXRDP` で確認すること。

---

## 使い分けの目安

| 要件 | 推奨 |
|---|---|
| 1時間に1回データ送信、着信不要 | **PSM**（TAU=1h, Active=10s） |
| 数分おきにデータ送信 | **PSM**（TAU=数分） |
| 1分以内の着信レイテンシが必要 | **eDRX**（サイクル=20〜60秒） |
| リアルタイム通信 | PSM/eDRX なし（常時接続） |
| 極限の電池節約 | **PSM** + 長い TAU |

---

## SORACOM/KDDI 実測値（参考）

sIoTamago + SORACOM plan-KM1 + KDDI LTE-M 環境での実測値（2026-03-24）。

| | 要求値 | ネットワーク割り当て値 | 備考 |
|---|---|---|---|
| T3324 (Active Time) | `00000101`（10秒） | `00001000`（16秒） | 少し伸張された |
| T3412 (TAU) | `10100001`（1分） | `10111111`（31分） | 大幅に伸張された |

> `AT+CEREG=4` + `AT+CEREG?` で確認。レスポンス末尾2フィールドが T3324/T3412。
>
> ネットワーク割り当て TAU が要求値を大幅に超える場合があるため、
> PSM テストの復帰待ちタイムアウトは実測 TAU に合わせて設定すること。

---

## sIoTamago テストコマンド

| コマンド | 内容 | 所要時間 |
|---|---|---|
| `p` | PSM テスト（スリープ検出→TAU復帰→HTTP GET） | 約90秒 |
| `e` | eDRX テスト（接続維持確認→HTTP GET） | 約30秒 |
